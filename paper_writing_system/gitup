### 로컬 프로젝트를 등록하기 전, GitHub 웹사이트에서 새로운 저장소(New Repository)를 생성해야 한다. 
### 이때 Initialize this repository with 옵션(README, .gitignore 등)은 모두 체크 해제한 **상태(Empty Repository)**로 생성하는 것을 권장한다. 
### 로컬의 이력과 충돌을 방지하기 위함이다.


git init

git add .

git commit -m "initial commit"

git branch -M main

